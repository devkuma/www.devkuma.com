<!DOCTYPE html>
<html lang="ko">
<head>
  <meta charset="UTF-8">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script>
  $(function() {
    $("button").click(function(){
      // setter
      $("img").width(300);
      $("img").height(300);

      // getter
      var imgWidth = $("img").width();
      var imgHeight = $("img").height();
      $("p").html("가로 길이:" + imgWidth + ", 세로 길이:" + imgHeight);
    });
  });
  </script>
</head>
<body>
  <h1>jQuery .attr()</h1>
  <img src="/resources/images/logo/128x128.png" width="128" height="128">
  <p></p>
  <button>이미지 크기 변경</button>
</body>
</html>